{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/build-blog.js && vite",
    "build": "node scripts/build-blog.js && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.161.0",
    "gsap": "^3.12.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
